# Copy to `.env` for local development. `.env` is gitignored.
#
# Astro only exposes `PUBLIC_`-prefixed variables to the browser. Everything
# else is server-only and is read from the Cloudflare Worker environment in
# production (Workers & Pages → tilershub → Variables and Secrets).

# ── Public (safe to ship to the browser) ──────────────────────────────────────

# Google AdSense publisher ID, including the "ca-" prefix.
# Leave blank to disable AdSense entirely — the head tags and every AdSlot are
# skipped when this is empty, so local and preview builds serve no ads.
PUBLIC_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX

# ── Server-only ───────────────────────────────────────────────────────────────

SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=

# ── SMS (Text.lk) ─────────────────────────────────────────────────────────────
#
# Set these in Cloudflare → Workers & Pages → wedahub → Variables and Secrets.
# With TEXTLK_API_KEY unset the app runs in stub mode: messages are written to
# sms_log as 'stubbed' and logged instead of sent, so every flow is testable
# without a gateway account.
#
# SMS_SENDER_ID must be a sender name Text.lk has approved for the account —
# alphanumeric, 11 characters maximum.
TEXTLK_API_KEY=
SMS_SENDER_ID=
