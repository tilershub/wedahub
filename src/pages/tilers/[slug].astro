---
import { districtBySlug } from '../../lib/locations.js'
const district = districtBySlug(Astro.params.slug)
return Astro.redirect(district ? `/providers?district=${encodeURIComponent(district.name)}` : `/providers/${Astro.params.slug}`, 301)
---
